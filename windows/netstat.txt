netstat
netstat -?
netstat -n         [numbers (443), not names(https)] faster because it does not resolve dns
netstat -a         [every port listening for a connection]
netstat -an
netstat -ar
netstat -b         [binaries, shows which applications are using the ports] requires elevation
netstat -f         [resolves dns of foreign addresses]
netstat -bf        [both]



